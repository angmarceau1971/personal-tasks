# Google Cloud Firestore for persistent data storage
google-cloud-firestore==2.13.1

# No other external dependencies required
# The task dashboard uses Python standard library modules:
# - http.server
# - socketserver  
# - json
# - webbrowser
# - os
# - pathlib

# This file is required for Docker build but can be empty
# since we only use standard library modules 